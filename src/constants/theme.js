// Theme file combining all styles constants

import COLORS from './colors';
import SIZES from './sizes';
import FONTS from './fonts';

export { COLORS, SIZES, FONTS };

const theme = {
  COLORS,
  SIZES,
  FONTS,
};

export default theme; 