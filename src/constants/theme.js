// Theme file combining all styles constants

import COLORS from './colors';
import RESTAURANT_COLORS from './restaurantColors';
import SIZES from './sizes';
import FONTS from './fonts';

export { COLORS, RESTAURANT_COLORS, SIZES, FONTS };

const theme = {
  COLORS,
  RESTAURANT_COLORS,
  SIZES,
  FONTS,
};

export default theme; 